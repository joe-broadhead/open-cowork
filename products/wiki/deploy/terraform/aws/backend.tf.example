terraform {
  backend "s3" {
    bucket         = "openwiki-terraform-state"
    key            = "openwiki/aws/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "openwiki-terraform-locks"
  }
}
