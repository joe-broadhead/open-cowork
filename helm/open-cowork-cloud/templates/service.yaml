{{- if .Values.roles.web.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-cowork-cloud.fullname" . }}-web
  labels:
    {{- include "open-cowork-cloud.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-cowork-cloud.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
{{- end }}
