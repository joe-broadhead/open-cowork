// Single source lives in @open-cowork/ui so desktop screens and the shared studio
// surfaces (and the website) all assign the same identity hue to an entity. Pair
// with the `.entity-tile` recipe in the shared stylesheet.
export { entityChroma } from '@open-cowork/ui'
